target-version = "py311"
line-length = 100

extend-exclude = [
    "alembic/versions",
]

[lint]

# Enforce real Python correctness + clean imports.
# Historical style modernization will be handled gradually.
select = [
    "E4",
    "E7",
    "E9",
    "F",
    "I",
]
